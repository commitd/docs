{
  "name": "@committed/gatsby-theme-docs-workspace",
  "private": true,
  "version": "1.0.4",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "yarn workspace example clean",
    "start": "yarn develop",
    "develop": "yarn workspace example develop",
    "build": "yarn workspace example build",
    "serve": "yarn workspace example serve",
    "deploy": "yarn workspaces run deploy",
    "bump": "yarn version --patch --no-git-tag-version ; yarn workspaces run version --patch --no-git-tag-version"
  },
  "workspaces": [
    "theme",
    "example"
  ]
}
