---
title: "Example mermaid"
order: "3"
---

# Example Mermaid

You can add diagrams using mermaid e.g.:

````
```mermaid
graph LR
install[Install Theme]
install --> configure[Configure]
configure --> draw[Write Docs With Diagrams]
```
````

gives:

```mermaid
graph LR
install[Install Theme]
install --> configure[Configure]
configure --> draw[Write Docs With Diagrams]
```
